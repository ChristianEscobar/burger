<div class="row">
	<div class="col-md-3"></div>
	<div class="col-md-4">
		<ol>
			{{#each burgers}}
				{{#unless this.devoured}}
					<li>
						{{ this.burger_name }}
					</li>
				{{/unless}}
			{{/each}}
		</ol>
	</div> <!-- .col-md-4 -->
	<div class="col-md-4">
		<ol>
			{{#each burgers}}
				{{#if this.devoured}}
					<li>
						{{ this.burger_name }}
					</li>
				{{/if}}
			{{/each}}
		</ol>
	</div> <!-- .col-md-4 -->
</div> <!-- .row -->