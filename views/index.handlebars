<div class="row">
	<div class="col-md-3"></div>
	<div class="col-md-9">
		<ol>
			{{#each burgers}}
				{{#unless this.devoured}}
					<li>
						{{ this.burger_name }}
					</li>
				{{/unless}}
			{{/each}}
		</ol>
	</div>
</div> <!-- .row -->